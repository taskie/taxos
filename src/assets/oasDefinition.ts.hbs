/* eslint-disable @typescript-eslint/no-explicit-any */

{{#each x-taxos.tsRefs}}
import {{{ @key }}} from "{{{ this }}}";
{{/each}}

{{#if description}}/** {{ description }} */{{/if}}
{{#if items}}
export type {{{ x-taxos.key }}} = {{{ items.x-taxos.tsType }}}[];
{{/if}}
{{#unless items}}
export default interface {{{ x-taxos.key }}} {
  {{#each x-taxos.tsProperties}}
    {{#if description}}/** {{ description }} */{{/if}}
    {{{ @key }}}: {{{ tsType }}};
  {{/each}}
}
{{/unless}}
